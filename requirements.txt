accelerate
appdirs
loralib
bitsandbytes
black
black[jupyter]
datasets
fire
git+https://github.com/huggingface/peft.git
transformers>=4.28.0
sentencepiece
gradio
cupy-cuda11x>=12.0.0
pandas<1.6.0dev0,>=1.3
protobuf<5,>=4.21
pyarrow==11.*
dask==2023.7.1
fsspec==2024.3.0
apache-beam==2.34.4
jupyterlab==3.6.0
jupyter-lsp==2.0.0
cudf==11.7.1
dask==2024.3.1
pandas==2.0.0
google-cloud-aiplatform==1.22.2
google-cloud-storage==2.2.1
protobuf==3.13.0
shapely==1.8.5
tensorflow==2.15.0
keras==2.15.0
# Upgrade conflicting packages
dill==0.3.8
numpy==1.25.0
pyarrow==10.0.0
jupyterlab==3.6.0
dask==2024.3.1
pandas>=2.0.0
protobuf==3.12.2
fsspec==2023.12.2
multiprocess==0.70.16

# Downgrade conflicting packages
apache-beam==2.45.0  # Downgrading to a version that doesn't conflict with current numpy and protobuf versions

# Remove conflicting packages
# Remove cubinlinker, ptxcompiler, cuda-python since they are not installed and causing conflicts

# Remove pathos since it requires newer versions of dill and multiprocess than what is specified
# Remove tensorflow-transform since it requires protobuf<4,>=3.7 which conflicts with the specified protobuf version

